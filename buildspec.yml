version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 16
  pre_build:
    commands:
      - echo pre-build...
  build:
    commands:
      - echo Build started on `date`
      - npm ci
      - npm run build
cache:
  paths:
    - './node_modules/**/*'
